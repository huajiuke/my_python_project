# 常见坑清单

> 上级：[[05-面试/05.01-Python后端/05.01.06-常见坑与模板/05.01.06-常见坑与模板|05.01.06-常见坑与模板]]

1. 可变默认参数
2. 循环闭包延迟绑定
3. 字典遍历时修改
4. 浮点数精度
```python
0.1 + 0.2 == 0.3  # False
```
5. 链式赋值
```python
a = b = []
a.append(1)
# a 和 b 指向同一个 list
```
6. 用 `except:` 吞掉所有异常
7. 大列表直接 `+` 拼接，应该用 `extend` 或列表推导
8. 不理解深浅拷贝导致内层数据被修改
