# Request-ID 与日志中间件

> 上级：[[03-FastAPI/03.04-工程化实践/03.04.03-中间件实践/03.04.03-中间件实践|03.04.03-中间件实践]]

## 1. Request-ID 中间件
每个请求生成唯一 ID，方便排查日志：
```python
class RequestIDMiddleware(BaseHTTPMiddleware):
    async def dispatch(self, request: Request, call_next):
        request_id = request.headers.get("X-Request-ID") or str(uuid.uuid4())
        request.state.request_id = request_id
        response = await call_next(request)
        response.headers["X-Request-ID"] = request_id
        return response
```

## 2. 日志中间件
```python
class LogMiddleware(BaseHTTPMiddleware):
    async def dispatch(self, request: Request, call_next):
        start = time.time()
        response = await call_next(request)
        cost = round((time.time() - start) * 1000, 2)
        response.headers["X-Process-Time"] = str(cost)
        logger.info(
            "[%s] %s %s 耗时 %.2fms",
            request.state.request_id,
            request.method,
            request.url.path,
            cost,
        )
        return response
```

## 3. 中间件注册顺序
```python
app.add_middleware(CORSMiddleware, ...)
app.add_middleware(RequestIDMiddleware)
app.add_middleware(LogMiddleware)
```
FastAPI 后添加的中间件更外层，所以执行顺序是：
```text
LogMiddleware -> RequestIDMiddleware -> CORS -> 路由
```
